import 'package:contador/src/app.dart';
import 'package:flutter/material.dart';

// Nosotros escribimos
/* import 'src/app.dart'; */ // otra forma de import relativo

// Donde la app es lanzada, se ejecuta mediante el runApp
// se lanza una instancia de la clase MyApp que se encuentra en el src

void main() {
  runApp(MyApp());
}
